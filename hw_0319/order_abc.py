#https://dmoj.ca/problem/coci06c2p2
#
numbers = map(int, input("num1, num2, num3").split(" "))
order = input("A, B, C")

if int(input()) < 0 or int(input()) > 100:
    exit()

for char in order:
    if "A" < "B" < "C" < 100:
        "A" ==
        if order[] == "A":
            numbers = min(numbers)
        elif order[1] == "B":
            numbers = max(numbers)
        else:
            numbers = (min(numbers) + max(numbers)) //2

print(numbers)








